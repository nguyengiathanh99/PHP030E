<!-- Git_teamwork.php 
    Sau khi dc join vào dự án các member cần làm:
    lấy code của dự án về: 
    Tạo 1 brank mới và làm việc trên brand này
    git checkout -b <tên-brand-tự-đặt>
    vd: git checkout -b <tên-của-bạn>
     - Sau khi đó bắt đầu code chức năng: tạo 1 vài file demo....
     Làm lại các thao tác git add, git commit, và git push để đẩy code lên dự án chung
     tuy nhiên sẽ đẩy code vào chính brand vừa tạo
     vd: git push origin <tên-của-bạn>
     Sau khi push -> xuất hiện 1 link remote, copy link đó paste lên trình duyệt-> tự tạo
     pull request và tự merge code vào dự án
-->
<?php
    
?>